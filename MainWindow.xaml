<Window x:Class="PastelSdkClient32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pastel Partner SDK (32-bit) - WPF Test" Height="620" Width="1000"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Status: Idle"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding ProcessBitness, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Connection row -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Company Path:" VerticalAlignment="Center" Width="110"/>
                <TextBox x:Name="TxtCompany" Width="600" Margin="6,0,20,0"
                         Text="Q:\2026"/>
                <Button x:Name="BtnConnect" Content="1) Connect" Width="140" Margin="0,0,8,0" Click="BtnConnect_Click"/>
                <Button x:Name="BtnDisconnect" Content="Disconnect" Width="120" Click="BtnDisconnect_Click" IsEnabled="False"/>
            </StackPanel>

            <!-- Credentials row -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Text="User:" VerticalAlignment="Center" Width="110"/>
                <TextBox x:Name="TxtUser" Width="160" Margin="6,0,20,0" Text="D. DE WET"/>
                <TextBlock Text="Password:" VerticalAlignment="Center" Width="110"/>
                <PasswordBox x:Name="TxtPass" Width="160" Margin="6,0,20,0"/>
                <Button x:Name="BtnFetch" Content="2) Fetch Customers" Width="180" Click="BtnFetch_Click" IsEnabled="False"/>
                <Button x:Name="BtnExport" Content="Export CSV" Width="120" Margin="8,0,0,0" Click="BtnExport_Click" IsEnabled="False"/>
            </StackPanel>

            <!-- Data grid -->
            <DataGrid x:Name="GridCustomers" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Account}" Header="Account" Width="150"/>
                    <DataGridTextColumn Binding="{Binding Description}" Header="Description" Width="*"/>
                    <DataGridTextColumn Binding="{Binding Telephone}" Header="Telephone" Width="180"/>
                    <DataGridTextColumn Binding="{Binding Contact}" Header="Contact" Width="180"/>
                    <DataGridTextColumn Binding="{Binding Pipe}" Header="PipeDelimited" Width="300"/>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </DockPanel>
</Window>